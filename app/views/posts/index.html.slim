h2 スラング一覧
.container
  .slang_list
    - @posts.each do |post|
      .col-lg-4.col-md-6.col-sm-12
        .panel.panel-default
          .panel-heading
            = link_to post.name, post
          .panel-body
            = post.description
          .panel-footer
            .user_name.glyphicon.glyphicon-user
              = link_to post.user.name, post

.pagi_nation
  = paginate @posts
  /= page_entries_info @posts
